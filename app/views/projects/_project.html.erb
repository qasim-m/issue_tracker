<div id="<%= dom_id project %>" class="w-full mx-auto my-2 ">
  <div class="mb-4">
    <h2 class="text-2xl sm:text-3xl font-extrabold text-gray-900 break-words">
      <%= project.title %>
    </h2>
  </div>
  <div class="mt-4 flex flex-col sm:flex-row sm:items-center sm:justify-between text-gray-500 text-sm gap-2">
    <span>Created: <%= project.created_at.strftime("%b %d, %Y") %></span>
  </div>
</div>
