<% content_for :title, "New Project" %>

<div class="container mx-auto px-4 py-8 max-w-2xl">
  <div class="bg-white p-8 rounded-sm shadow-xl border border-gray-200">
    <div class="flex justify-between items-center mb-6">
      <h1 class="font-semibold text-3xl text-gray-900">New Project</h1>
      <%= link_to "Back to Projects", projects_path,
          class: "self-start text-center rounded-sm px-5 py-2 border-2 border-blue-600 text-blue-500 font-semibold shadow-sm transition hover:bg-blue-600 hover:text-white" %>
    </div>
    
    <%= render "form", project: @project %>
  </div>
</div>
